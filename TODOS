- for now we only partially recreate surface
  so when changing window from non HDR to HDR it will not work correctly
  it would need to additionally recreate the renderpass to make sure the change between dynamic ranges is properly reflected.
